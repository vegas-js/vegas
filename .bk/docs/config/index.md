# Configuring Vegas

When you run vegas from the command line, Vite will automatically try to load a configuration file named `vegas.config.ts` located in the project root.

The most basic config file looks like this:

::: code-group

```typescript [vegas.config.ts]
export default defineConfig({
  // config options
});
```

:::
