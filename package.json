{
  "name": "@vegasjs/vegas-monorepo",
  "private": true,
  "homepage": "https://vegasjs.dev",
  "license": "MIT",
  "author": "Yasushi",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vegas-js/vegas.git"
  },
  "type": "module",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "lint": "oxlint",
    "format": "oxfmt",
    "test": "vitest run",
    "coverage": "vitest run --coverage --coverage.include 'packages/*/src/**/*.ts' --coverage.reporter text",
    "build": "tsdown --config-loader unrun --workspace",
    "docs:dev": "pnpm --filter ./docs dev",
    "docs:build": "pnpm --filter ./docs build",
    "docs:preview": "pnpm --filter ./docs preview"
  },
  "devDependencies": {
    "@vegasjs/vegas": "workspace:*",
    "@vitest/coverage-v8": "^4.1.2",
    "oxfmt": "^0.43.0",
    "oxlint": "^1.58.0",
    "oxlint-tsgolint": "^0.19.0",
    "tsdown": "^0.21.7",
    "vitest": "^4.1.2"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@10.33.0"
}
